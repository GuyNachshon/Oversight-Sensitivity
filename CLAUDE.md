- use UV for python execution / dependency managment
- DO NOT WRITE mocks/placeholders/stubs. we aim to create the fullest implementation.

## Active Technologies
- Python 3.13 (per project pyproject.toml) + ransformers>=4.36.0 (model loading/hooks), torch>=2.1.0 (inference), numpy>=1.24.0, scipy>=1.11.0 (statistics), scikit-learn>=1.3.0 (linear models), matplotlib>=3.8.0, seaborn>=0.13.0 (visualization), pyyaml>=6.0 (configs) (001-oversight-sensitivity-measurement)
- File system (JSON for results/configs; CSV/JSON for prompts; PNG/PDF for figures) (001-oversight-sensitivity-measurement)

## Recent Changes
- 001-oversight-sensitivity-measurement: Added Python 3.13 (per project pyproject.toml) + ransformers>=4.36.0 (model loading/hooks), torch>=2.1.0 (inference), numpy>=1.24.0, scipy>=1.11.0 (statistics), scikit-learn>=1.3.0 (linear models), matplotlib>=3.8.0, seaborn>=0.13.0 (visualization), pyyaml>=6.0 (configs)
